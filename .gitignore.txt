# Brainfox Academy — .gitignore
# Files Git should NOT track

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# VS Code settings (optional to ignore)
.vscode/

# Node (if you ever add build tools later)
node_modules/
npm-debug.log

# Environment variables (never commit secrets)
.env
.env.local